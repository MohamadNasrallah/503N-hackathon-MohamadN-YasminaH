# Core data science
pandas>=2.1.0
numpy>=1.26.0,<2.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Association rule mining (combo optimization)
mlxtend>=0.23.0

# Time-series forecasting (optional — falls back to linear regression if missing)
# prophet>=1.1.5

# API backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Streamlit dashboard
streamlit>=1.35.0
plotly>=5.22.0

# Environment variable loading (.env file support) — keeps secrets out of code
python-dotenv>=1.0.0

# HTTP client (for dashboard -> API calls)
requests>=2.32.0

# Jupyter notebooks
jupyter>=1.0.0
ipykernel>=6.29.0
nbconvert>=7.16.0

# ── OPTIONAL: Gemini AI agent ─────────────────────────────────────────────
# Only needed for the "Ask the Agent" natural-language query endpoint.
# The dashboard and all 5 analytical models work without this.
# To enable: pip install google-generativeai  and  add GEMINI_API_KEY to .env
# google-generativeai>=0.7.0
